apiVersion: v1
kind: Pod
metadata:
   name: redis-pod
   labels:
      name: redis-pod
      app: demo-votingapp-pod
spec:
   containers:
     - name: redis-pod
       image: redis:latest
       ports: 
         containerPort: 6379 
